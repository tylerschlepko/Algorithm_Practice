var longestNiceSubstring = function(s) {
    let longStr = ''
    let allowedObj = {}
    let start = 0
    let end = s.length - 1
    while (start < end){
        s[start] 
        //see if end is = to the opposite lower or uppercase if the start letter 
        //if not it moves in
        //if it is make a new var that moves forward
        //if the capital letter or lowercase isn
    }
};

let s = "YazaAay"
// Output: "aAa"